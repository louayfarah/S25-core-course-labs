apiVersion: v1
kind: Service
metadata:
  name: timezone-nodejs-app
spec:
  type: NodePort
  selector:
    app: timezone-nodejs-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
