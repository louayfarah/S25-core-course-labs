apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nodejs-app.fullname" . }}-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
