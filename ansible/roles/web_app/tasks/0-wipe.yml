---
- name: Remove existing application container
  docker_container:
    name: web_app_container
    state: absent
  tags: [wipe]

- name: Remove deployed docker-compose file
  file:
    path: /opt/web_app/docker-compose.yml
    state: absent
  tags: [wipe]
